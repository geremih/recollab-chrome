'use strict';

console.log '\'Allo \'Allo! Content script'



$( document ).tooltip
                items: "[data-href]",
                content: ->
                        element = $( this );
                        if ( element.is( "[data-href]" ) ) 
                                text = element.text();
                                return text;
        alert "Its working"


